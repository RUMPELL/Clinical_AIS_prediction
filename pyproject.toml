[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "aiscode-sllm"
version = "0.1.0"
description = "AIS code prediction with sLLM (Qwen3-8B + LoRA)"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}
